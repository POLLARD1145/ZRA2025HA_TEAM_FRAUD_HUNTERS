# Update render.yaml to work from root (no cd commands needed)
@'
services:
  - type: web
    name: zra-sdk
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn zra_sdk.wsgi:app --bind 0.0.0.0:$PORT
'@ | Set-Content -Path "render.yaml" -Encoding utf8