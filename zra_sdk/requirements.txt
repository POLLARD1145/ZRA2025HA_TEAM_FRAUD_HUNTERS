# Replace your requirements.txt with the optimized version
@'
# Core web framework
Flask==2.3.3

# API and data handling  
requests==2.31.0
python-dotenv==1.0.0
pydantic==2.4.0

# Lightweight data processing (remove if not essential)
pandas==2.0.3
numpy==1.24.3
'@ | Set-Content -Path "requirements.txt" -Encoding utf8
